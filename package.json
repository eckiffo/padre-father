{
  "name": "padre-father",
  "version": "1.0.0",
  "description": "$PADRE — Father of Trenches church website, OBS overlays, and Blessing Score system",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@coin-communities/sdk": "latest",
    "@solana/web3.js": "^1.95.0",
    "@vercel/kv": "^3.0.0"
  },
  "devDependencies": {
    "vercel": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
